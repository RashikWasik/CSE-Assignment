//EXAMPLE 6.14 Consecutive Integer Quantities

#include <stdio.h>
main() /* display the numbers 0 through 9 */
{
    int digit;
    for (digit = 0; digit <= 9; ++digit)
        printf("%d\n", digit);
}
